{
  "name": "root",
  "version": "0.0.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "bootstrap": "pnpm i && lerna bootstrap",
    "build": "lerna run build",
    "test": "lerna run test --stream",
    "publish": "lerna publish"
  },
  "devDependencies": {
    "folio": "^0.3.18",
    "lerna": "^4.0.0",
    "typescript": "^4.4.4"
  }
}
